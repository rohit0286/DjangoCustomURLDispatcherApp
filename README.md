DjangoCustomURLDispatcherApp
============================

Django application for Custom Url Dispatcher and views.

The run the application import the code into eclipse. Run the project as Django application.
